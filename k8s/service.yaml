apiVersion: v1
kind: Service
metadata:
  name: resume-mcp-service
  namespace: resume
  labels:
    app: resume-mcp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: resume-mcp
